// 12.cpp : Defines the entry point for the console application.
//

/*
12.写代码验证bool和BOOL的区别。
*/

//#include "stdafx.h"
/*
int main(int argc, char* argv[])
{
	//bool nRet = false;
  bool nRet = true;
  //if(nRet == true)可以但不要这么写，很low
  if(nRet)
  {
    printf("bool true %d\r\n", nRet); //1
    printf("bool true %s\r\n", nRet); //
  }
  else
  {
    printf("bool false %d\r\n", nRet); //0
    printf("bool false %s\r\n", nRet); //(null)
  }
	return 0;
}
*/

/*
Cpp中bool类型就是一个逻辑值真或假的判定

C语法中没有bool类型，人们为了习惯使用，
将int类型定义为了BOOL类型,将1或者0定义为TRUE或者FALSE，
从而达到和Cpp bool类似的表式方式，
但是从根本上来看，并没有什么不同
*/

